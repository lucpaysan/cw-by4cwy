name: Release Build

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci

      - name: Build Tauri app (portable exe, no installer)
        working-directory: ./src-tauri
        run: |
          cargo tauri build --bundles app --config tauri.windows.conf.json

      - name: Upload Windows portable .exe
        uses: actions/upload-artifact@v4
        with:
          name: windows-portable-exe
          path: |
            src-tauri/target/release/bundle/app/*.exe
          if-no-files-found: error

  build-android:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android

      - name: Install dependencies
        run: npm ci

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build Tauri app (Android APK)
        working-directory: ./src-tauri
        run: |
          cargo tauri build --bundles apk --config tauri.android.conf.json

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: |
            src-tauri/target/release/bundle/android/*.apk
          if-no-files-found: error
